# Core — required for the pipeline to import and run
pronouncing>=0.2.0        # CMUdict wrapper used by the rule-based G2P source
panphon>=0.19             # phonological-feature distance for the confidence scorer
anthropic>=0.40.0         # LLM phonetician source (set ANTHROPIC_API_KEY)
python-dotenv>=1.0.0

# Dev / test
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional — multilingual rule-based G2P (needs system data files for some
# languages; install separately if you want non-English coverage in Source 1)
# epitran>=1.24

# Optional — neural G2P source. Heavy (pulls in torch). Install with:
#   pip install -r requirements.txt && pip install deep-phonemizer torch
# The pipeline runs fine without it — neural_g2p.py degrades to "unavailable"
# and the ensemble falls back to the rule-based + LLM sources.
# deep-phonemizer>=0.0.19
# torch>=2.2

# Optional — experiment tracking for the stretch fine-tune (training/)
# wandb>=0.17
