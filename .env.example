# Copy to .env and fill in. Never commit the real .env file.

# Required for src/truvox/sources/llm_phonetician.py
ANTHROPIC_API_KEY=

# Optional — only needed once you wire up the TTS benchmark
ELEVENLABS_API_KEY=
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=

# Optional — stretch fine-tune experiment tracking
WANDB_API_KEY=
